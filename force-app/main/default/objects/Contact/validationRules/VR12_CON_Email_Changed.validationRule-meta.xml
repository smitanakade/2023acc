<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR12_CON_Email_Changed</fullName>
    <active>true</active>
    <description>Ensures a populated email cannot be changed to be blank.</description>
    <errorConditionFormula>AND(ISCHANGED(Email), ISBLANK(Email))</errorConditionFormula>
    <errorMessage>Portal User must have an email address to receive system notifications</errorMessage>
</ValidationRule>
